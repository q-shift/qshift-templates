apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: quarkus-demo
  description: "A collection of the Backstage resources: org, user, component, templates for the Quarkus demo"
spec:
  targets:
    - ./org.yaml
    - ./entities.yaml
    - ./component.yaml
    - ./templates/quarkus-extensions/template.yaml
    - ./templates/quarkus-quickstart-picker/template.yaml